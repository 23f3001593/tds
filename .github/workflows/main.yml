name: TDS Task Action

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f3001593@ds.study.iitm.ac.in
        run: echo "Hello, world!"